import { ProductListingPage } from "@/components/ProductListingPage";

export default function BoosterBoxesPage() {
  return (
    <ProductListingPage
      category="sealed"
      description="Browse sealed booster boxes for your next rip or collection."
      keywords={["booster box"]}
      title="Booster Boxes"
    />
  );
}
